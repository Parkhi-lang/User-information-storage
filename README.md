📌 Project Overview

This project is a simple REST API built with Flask that performs CRUD (Create, Read, Update, Delete) operations on user data.

The application stores users in memory using a Python dictionary and exposes RESTful endpoints to manage them.

🎯 Objective

Build a REST API using Flask

Implement GET, POST, PUT, DELETE routes

Understand API development fundamentals

🛠 Tech Stack

Python 3.x

Flask

Postman / cURL (for testing)

